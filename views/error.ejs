<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error | Something Went Wrong</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: #f9f9f9;
            color: #444;
            text-align: center;
            padding: 0;
            margin: 0;
        }
        .error-container {
            max-width: 700px;
            margin: 30px auto;
            padding: 30px;
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .error-icon {
            font-size: 120px;
            color: #e74c3c;
            margin-bottom: 20px;
        }
        .error-code {
            font-size: 120px;
            font-weight: 700;
            color: #e74c3c;
            margin: 0;
        }
        .error-message {
            font-size: 28px;
            font-weight: 500;
            color: #333;
            margin: 10px 0;
        }
        .error-details {
            font-size: 18px;
            color: #666;
            margin: 10px 0;
        }
        .home-link {
            display: inline-block;
            margin-top: 30px;
            padding: 12px 25px;
            background: #3498db;
            color: #fff;
            text-decoration: none;
            border-radius: 8px;
            font-size: 20px;
            transition: background 0.3s, transform 0.3s;
        }
        .home-link:hover {
            background: #2980b9;
            transform: scale(1.05);
        }
        .home-link:focus {
            outline: none;
        }
    </style>
</head>
<body>
    <div class="error-container">
        <i class="error-icon fas fa-exclamation-circle"></i>
        <h1 class="error-code"><%= errorCode %></h1>
        <p class="error-message"><%= errorMessage %></p>
        <p class="error-details">Oops! Something went wrong. Please try again later or contact support if the problem persists.</p>
        <a href="/auth/v3/login" class="home-link">Go to Login</a>
    </div>
</body>
</html>
